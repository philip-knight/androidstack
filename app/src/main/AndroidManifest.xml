<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidstack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui_layout.TableLayoutActivity"></activity>
        <activity android:name=".ui_layout.GridActivity" />
        <activity android:name=".ui_layout.RelativeLayoutActivity" />
        <activity android:name=".ui_layout.FrameLayoutActivity" />
        <activity android:name=".ui_layout.UILayoutCenter" />
        <activity android:name=".ui_layout.LinearLayoutActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>