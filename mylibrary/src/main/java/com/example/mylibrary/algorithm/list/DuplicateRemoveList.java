package com.example.mylibrary.algorithm.list;

/**
 * Created by Philip.Knight.
 * User Email: 411735025@qq.com
 * Date: 2019-12-11
 * Time: 16:20
 * Desc:
 * Version:
 */
/*
public class DuplicateRemoveList {
    public static void main[String[] args]{
        String[] arr = {"xxx","sds","ss","dd","nn","mm","mm","sds"};
            // 方法一，将数据放入 List 里面
        List<String> list = new ArrayList<>();
        for (int i = 0; i < arr.length; i++){
            if (!list.contains(arr[i])){
                list.add(arr[i]);
            }
        }

        Set<String> set = new HashSet<>(Arrays.asList(arr));
        Iterator<String> it  = set.iterator();
        while (it.hasNext()){
            System.out.println(it.next());
        }

        List<String> listStr = new ArraryList<>(arr);
        for(int i = 0;i < listStr.size();i++){
            for(int j = listStr.size - 1; j > i; j--){
                if (listStr.get(i).equals(listStr.get(j))){
                    listStr.remove(j);
            }
        }
    }

}

 */
